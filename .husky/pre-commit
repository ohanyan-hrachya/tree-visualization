#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🧵 Running lint-staged..."
pnpm lint-staged

echo "🔍 Running type check..."
pnpm typecheck

echo "🧪 Running tests..."
pnpm test

echo "🏗️ Building app..."
pnpm build

echo "🚦 Running Lighthouse performance check..."
# Use a repo-local temp directory to avoid Windows temp cleanup permission issues.
export TMPDIR="$(pwd)/.lighthouseci/tmp"
mkdir -p "$TMPDIR"
pnpm lh:check
